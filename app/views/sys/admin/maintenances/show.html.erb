<%=raw init_tiny_mce :readonly => true %>

<table class="show">
  <tr>
    <th><%= @item.locale(:state) %></th>
    <td><%=h safe{@item.status.name} %></td>
  </tr><tr>
    <th><%= @item.locale(:published_at) %></th>
    <td><%=h safe{@item.published_at.strftime("%Y-%m-%d %H:%M")} %></td>
  </tr><tr>
    <th><%= @item.locale(:title) %></th>
    <td><%=h @item.title %></td>
  </tr>
</table>

<table class="show">
  <tr>
    <th><%= @item.locale(:body) %></th>
  </tr><tr>
    <td><%= text_area_tag 'body', @item.body, :class => 'mceEditor', :style => 'width:100%' %></td>
  </tr>
</table>
