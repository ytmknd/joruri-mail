## Dovecot configuration for Joruri Mail test environment
## Replaces Greenmail for IMAP + LMTP delivery

protocols = imap lmtp

# Listen on custom ports to match Greenmail config
listen = *

!include conf.d/*.conf
