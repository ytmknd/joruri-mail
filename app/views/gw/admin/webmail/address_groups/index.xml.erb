<?xml version='1.0' encoding='UTF-8'?>
<items>
  <count><%= @s_items.size %></count>
  <% @s_items.each do |item| %>
  <item>
    <id><%= item.id %></id>
    <name><%= item.name %></name>
    <email><%= item.email %></email>
  </item>
  <% end %>
</items>
