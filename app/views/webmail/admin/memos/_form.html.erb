<%= error_messages_for @item %>

<table class="show">
  <tr>
    <td><%= text_area_tag :memo_body, @item.value, class: 'body' %></td>
  </tr>
</table>

<div class="submitters">
  <%= submit_tag '編集する' %>
</div>
