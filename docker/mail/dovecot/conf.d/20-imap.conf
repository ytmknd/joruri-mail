## IMAP service — listen on port 3143 (matches legacy imap.yml)
service imap-login {
  inet_listener imap {
    port = 3143
  }
  # Disable IMAPS (no TLS in test environment)
  inet_listener imaps {
    port = 0
  }
}

## IMAP protocol settings
protocol imap {
  imap_client_workarounds =
}
