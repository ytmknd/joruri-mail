<% @addresses.each do |key, addrs| %>
  <% addrs.each do |addr| %>
    AddressSelector.instance.add('<%= key %>',
      '<%=raw escape_javascript addr.display_name %>',
      '<%=raw escape_javascript addr.address %>');
  <% end %>
<% end %>
