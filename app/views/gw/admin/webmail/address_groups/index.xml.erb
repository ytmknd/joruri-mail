<% items = @s_items || @items %>
<?xml version='1.0' encoding='UTF-8'?>
<items>
  <count><%= items.size %></count>
  <% items.each do |item| %>
  <item>
    <id><%= item.id %></id>
    <name><%= item.name %></name>
    <email><%= item.email %></email>
  </item>
  <% end %>
</items>
