<?xml version='1.0' encoding='UTF-8'?>
<items>
  <group>
    <id><%= @item.id %></id>
    <name><%= @item.name %></name>
  </group>
  <% @items.each do |u| %>
  <item>
    <id><%= u.id %></id>
    <name><%= u.name %></name>
    <kana><%= u.kana %></kana>
    <email><%= u.email %></email>
    <sort_no><%= u.sort_no %></sort_no>
  </item>
  <% end %>
</items>