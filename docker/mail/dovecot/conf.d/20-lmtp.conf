## LMTP service — Postfix delivers mail here via Unix socket
service lmtp {
  unix_listener /var/spool/postfix/private/dovecot-lmtp {
    mode  = 0600
    user  = postfix
    group = postfix
  }
}

protocol lmtp {
  postmaster_address = postmaster@localhost.localdomain.jp
}
