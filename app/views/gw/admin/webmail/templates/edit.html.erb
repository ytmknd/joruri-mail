<div id="templateContent">
<%= render 'menu' %>

<%= form_for @item, as: :item, url: { action: :update }, html: { id: 'item_form', method: :patch } do |f| %>
  <%= render 'form', f: f %>
<% end %>

</div>
