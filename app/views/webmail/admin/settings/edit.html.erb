<div id="settingContent">
<%= render 'menu' %>

<%= form_for @item, as: :item, url: { action: :update }, html: { method: :patch, multipart: true, id: 'itemForm' } do |f| %>
  <%= render 'form', f: f %>
<% end %>

</div>
